# Chess Platform MVP Scaffold

Monorepo scaffold for an online chess platform.

## Stack
- Next.js (web)
- NestJS (api)
- PostgreSQL + Redis
- Prisma

## Quick start
```bash
cp .env.example .env
docker compose up -d postgres redis
```
